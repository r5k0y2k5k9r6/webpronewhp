jQuery(function($){
  $('.slider').each(function(){
  $(this).slick({
    autoplay:true,
    // autoplaySpeed:1000,
    dots:true,
    arrows:false,
    centerMode: true,
    centerPadding: '25%',
  });
});	
});	